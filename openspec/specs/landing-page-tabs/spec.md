## MODIFIED Requirements

### Requirement: Tab Navigation Snapping
Tabs in the landing page should snap nicely when scrolling.

#### Scenario: Swiping or scrolling through tabs horizontally
- **WHEN** the user scrolls horizontally through the landing page tabs
- **THEN** the scroll view snaps smoothly to the nearest tab boundary using CSS `scroll-snap`.
