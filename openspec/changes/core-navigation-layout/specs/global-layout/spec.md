## MODIFIED Requirements

### Requirement: Global Scrollbar Removal
Global scrollbars should be hidden to create a native app-like aesthetic.

#### Scenario: Viewing a scrollable page
- **WHEN** the content overflows vertically or horizontally
- **THEN** the content can be scrolled, but no visible scrollbar is rendered by the browser on the `body` or main container.
